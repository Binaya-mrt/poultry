import 'package:flutter/material.dart';

class UsersAccount extends StatelessWidget {
  const UsersAccount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('User details screen'),
        ),
      ),
    );
  }
}
